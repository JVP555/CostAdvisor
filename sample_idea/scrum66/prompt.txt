Estimate: 5 pts · stretch

Before a supplier call, the full read on a product — what it should cost, how that number has moved, whats actually driving the market right now — usually lives in a few scattered spreadsheets and the buyers own head. Intelligence is the one screen that pulls all of it into a single per-product dossier they can read before the call. Even read-only, with the forecast band stubbed and the grading engine still coming in Wave 3, this shell already hands them that brief — and it finally gives our derived should-cost history and the researched market narrative a real place to live instead of a mockup.

Build the Intelligence tab shell to the new mockup (new docs/intelligence_mockup.html is the working spec). Render derived history + stored narrative, read-only. The grading / signals / editorial engine is Wave 3.

Acceptance criteria:

Intelligence renders the mockup's panels from derived/stored data; the forecast band is stubbed (Forecast engine empty).

Note: the expert_reviewed checkbox needs backend persistence (currently in-memory only) before operational review tracking.

Sub-tasks:

Intelligence layout/panels to mockup

Render derived history + stored narrative (read-only)

Stub forecast band

Flag expert-reviewed persistence as a dependency

