Estimate: 5 pts · stretch

Every team ends up wanting a slightly different catalog — their own subfamily quirks, tweaked formulas, products they buy in a way we dont ship by default. DB-1 built the machinery to let a team copy our shared catalog into their own private version, but until this tab exists theres nowhere to actually do it — no way to browse whats there, pick what to make your own, and manage what youve changed. The important bit is it always shows what came from us versus what the team edited themselves, so when we push an improvement to the shared catalog, a team knows exactly which of their changes are theirs to protect.

Rebuild the Team tab to the new IA, and surface taxonomy fork management (from DB-1) — a team can view the platform taxonomy and fork families/subfamilies into an editable team copy.

Acceptance criteria:

Team tab renders existing team-management functions in the new IA.

A team can fork a platform family/subfamily and edit the copy (origin_id preserved).

Sub-tasks:

Team layout to new IA

Taxonomy fork management UI (from DB-1)

Show platform-vs-team origin lineage

Reference docs: Platform vs Team — how forking works