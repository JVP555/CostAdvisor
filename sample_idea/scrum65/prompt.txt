Estimate: 5 pts · committed core

Negotiate is where the whole journey actually pays off — every step before it exists so a category manager can walk into a supplier call with evidence instead of a gut feeling. If the restructure moves all the other tabs across but leaves this one behind, the journey dead-ends at 'heres a gap' and never gets to 'heres the brief i argue from.' Were not adding new negotiation smarts in Wave 2 — the job is just to land the existing product → gap → exportable-brief flow cleanly in the new layout, since this is the tab a buyer opens right before the conversation that actually saves them money.

Rebuild Negotiate to the new IA, wired to existing negotiation/brief outputs. New depth (cheat-sheet, tornado, PDF extraction) is Wave 3.

Acceptance criteria:

Negotiate renders the existing end-to-end flow (product → gap → exportable brief) in the new IA.

No new backend engine introduced.

Sub-tasks:

Negotiate layout to new IA

Wire to existing brief/export

Empty / loading states

