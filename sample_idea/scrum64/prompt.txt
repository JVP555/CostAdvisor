Estimate: 5 pts · stretch

The decision that actually matters to a buyer isnt just what a product costs today — its buy now or wait, lock a contract or hold off, and that turns entirely on where the price is heading. Forecast is where that question lives, sitting right between watching todays number in Monitor and walking into Negotiate. The engine that actually projects prices is Wave 3, but if we ship the journey with a hole where Forecast belongs, that decision just goes unanswered — so we stand its home up now, with the forward-looking part deliberately left blank rather than faked, because a made-up forecast is honestly worse than an honest 'we dont know yet.'

Build the Forecast tab shell to the new IA. The forward-projection engine is Wave 3 — render the layout with the dotted-forward-line placeholder stubbed or hidden.

Acceptance criteria:

Forecast tab exists in the nav with its layout.

No fabricated forecast numbers — the forward band is clearly stubbed/hidden until the engine lands.

Sub-tasks:

Forecast layout to new IA

Dotted-forward-line placeholder (toggle), stubbed

Wire index history (real) + stub the projection

